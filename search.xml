<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>改造 Vip</title>
      <link href="2021/03/16/vip/"/>
      <url>2021/03/16/vip/</url>
      
        <content type="html"><![CDATA[<h2 id="问题所在"><a href="#问题所在" class="headerlink" title="问题所在"></a>问题所在</h2><blockquote><ul><li>全部支付走统一的二维码生成接口，导致通过 type 区分需要的字段，支付方式越来越多，判断也越来越多，难以维护</li><li>代码解构混乱，一个 <code>$data</code> 变量贯通整个方法，导致最后不知道 <code>$data</code> 变量里面什么数据，开发、排错越来越复杂</li><li>异常处理，业务代码处处抛出 <code>Exception</code> 和捕获 <code>\Exception</code> ，导致真正程序的异常无法定位异常</li></ul></blockquote><h2 id="改造前的获取二维码接口"><a href="#改造前的获取二维码接口" class="headerlink" title="改造前的获取二维码接口"></a>改造前的获取二维码接口</h2><p>存在问题：</p><pre><code>1. 所有业务逻辑错误也抛出 `\Exception` 异常，捕获 `\Exception` 后返回 `下单失败` 导致如果程序遇到真正错误时，无法及时排查错误   2. 单看 `checkVerifyType()` 方法名会认为只是检查支付 `type` 是否正确， 但却不是，这个方法把所有该干不该干的事都干完了   3. 传参用 `0` ，`1` 也不知道是个啥   4. `qrcode` 接口参数也很复杂，例：`type` = 1时，必须要 `code` 参数； `type` = 2 时，必须要 `price` 参数；`type` = 3 时  ....   5. `$data` 里面各种数据，有：请求数据，订单临时数据，订单预览数据，根据购买商品的不同又放入不同的数据，结果 `$data` 就是个大杂烩，修改起来实在一言难尽</code></pre><pre><code class="php">public function qrcode(Request $request)&#123;    try &#123;          // ...        $key = $this-&gt;checkVerifyType(0, 1);          // ...        return $key;    &#125; catch (\Exception $e) &#123;        return &#39;下单失败&#39;;    &#125;&#125;</code></pre><pre><code class="php">private function checkVerifyType($payType1 = 0, $payType2 = 0)&#123;    $data = request()-&gt;all();    if (!ctype_digit(strval($data[&#39;type&#39;]))) &#123;        throw new \Exception(&#39;type err&#39;);    &#125;    switch ($data[&#39;type&#39;]) &#123;        case &#39;vip&#39;:            // ... 验证            $data[&#39;vip_info&#39;] = Vip::where(&#39;code&#39;, $data[&#39;code&#39;])-&gt;first();            break;        case &#39;recharge&#39;:            // ... 验证            $data[&#39;money&#39;] = $data[&#39;money&#39;];            break;        // case...    &#125;    // 优惠券判断    if ($data[&#39;coupon_id&#39;]) &#123;        $money = Coupon::where(&#39;id&#39;, $data[&#39;coupon_id&#39;])-&gt;value(&quot;money&quot;);        $data[&#39;reduce&#39;] = $money;        // ....    &#125;    // 订单预览信息    $data[&#39;show_title&#39;] = &quot;购买一个会员&quot;;    $data[&#39;show_money&#39;] = 100;    $key = &quot;abcdefg&quot;;    Redis::set($key, $data);    return $key;&#125;</code></pre><h2 id="改造"><a href="#改造" class="headerlink" title="改造"></a>改造</h2><blockquote><ol><li><p>涉及支付的模块有：开通会员、充值、购买单个商品等</p></li><li><p>开发支付流程：</p><ol><li>生成二维码（生成临时订单 <code>redis</code>，返回 <code>redis</code> 零时订单 <code>key</code>）</li><li>手机端确认购买信息（展示购买商品信息）</li><li>手机端确认支付 （通过临时订单的 <code>key</code> ，创建一条订单数据到数据库）</li></ol></li><li><p>创建订单和预览订单使用 <code>策略模式</code></p></li></ol></blockquote><h3 id="按模块区分不同的下单链接"><a href="#按模块区分不同的下单链接" class="headerlink" title="按模块区分不同的下单链接"></a>按模块区分不同的下单链接</h3><ol><li><p>开通会员： <code>/buy/vip</code></p></li><li><p>充值：<code>/buy/recharge</code></p></li><li><p>购买商品：<code>/buy/goods</code></p></li></ol><h3 id="策略模式"><a href="#策略模式" class="headerlink" title="策略模式"></a>策略模式</h3><p>Ref: <a href="https://juejin.cn/post/6844903748788027400">策略模式原来这么简单！</a></p><h3 id="前期准备"><a href="#前期准备" class="headerlink" title="前期准备"></a>前期准备</h3><p>在 <code>BaseContrller</code> 中增加几个返回数据的方法，方便调用</p><pre><code class="php">const SUCCESS_CODE = 200;const SUCCESS_FAIL = 100;protected function success($msg = &#39;ok&#39;, $data = [], $code = self::SUCCESS_CODE)&#123;    return [&#39;status&#39; =&gt; $code, &#39;message&#39; =&gt; $msg, &#39;data&#39; =&gt; $data];&#125;protected function data($data = [], $msg = &#39;ok&#39;, $code = self::SUCCESS_CODE)&#123;    return [&#39;status&#39; =&gt; $code, &#39;message&#39; =&gt; $msg, &#39;data&#39; =&gt; $data];&#125;protected function fail($msg = &#39;ok&#39;, $data = [], $code = self::SUCCESS_FAIL)&#123;    return [&#39;status&#39; =&gt; $code, &#39;message&#39; =&gt; $msg, &#39;data&#39; =&gt; $data];&#125;</code></pre><h3 id="创建订单策略"><a href="#创建订单策略" class="headerlink" title="创建订单策略"></a>创建订单策略</h3><ol><li><p>创建一个订单的 <code>抽象策略</code>，定义算法的接口，所有策略必须实现临时订单的接口，</p><p> app/Http/Services/PayOrder/PayOrderStrategy.php</p><pre><code class="php">abstract class PayOrderStrategy&#123;    abstract function createTemporaryOrder($request);&#125;</code></pre></li><li><p>创建一个 <code>Context</code> 类</p><p> app/Http/Services/PayOrder/PayOrderStrategy.php</p><pre><code class="php">class PayOrderContext&#123;    private $strategy;    public function __construct(PayOrderStrategy $payOrderStrategy)    &#123;        return $this-&gt;strategy = $payOrderStrategy;    &#125;    public function createOrder(Request $request)    &#123;        return $this-&gt;strategy-&gt;createTemporaryOrder($request);    &#125;&#125;</code></pre></li><li><p>创建具体的支付策略（vip 策略）</p><p> app/Http/Services/PayOrder/Strategy/VipStrategy.php</p><pre><code class="php">// 开通 vipclass VipStrategy extends PayOrderStrategy&#123;    function createTemporaryOrder(Request $request)    &#123;        $packageCode = $request[&#39;code&#39;];        $package     = app(PayOrderService::class)-&gt;getVipByCode($packageCode);        // 临时订单数据           $tmpOrder = [            &#39;package_cope&#39; =&gt; $package-&gt;toArray(),            &#39;type&#39;         =&gt; PayOrderService::TYPE_VIP,            &#39;uid&#39;          =&gt; 1,            &#39;ip&#39;           =&gt; $request-&gt;ip(),            // ....        ];        return app(PayOrderService::class)-&gt;saveTemporaryOrder($tmpOrder);    &#125;&#125;</code></pre></li></ol><pre><code>创建一个订单服务类，写一些创建订单的公共方法app/Http/Services/PayOrderService.php```use Ramsey\Uuid\Uuid;class PayOrderService&#123;    const TYPE_VIP      = 1; // 购买 vip    const TYPE_RECHARGE = 2; // 充值    const TYPE_GOODS    = 3; // 购买商品    // 通过 code 查询 vip 套餐信息    public function getVipByCode(string $code)    &#123;        // 这里应是从数据库获取数据返回        return collect([&#39;id&#39; =&gt; 1, &#39;code&#39; =&gt; &#39;vip1&#39;, &#39;price&#39; =&gt; 100, &#39;vip_day&#39; =&gt; 30]);    &#125;    // 保存临时订单    public function saveTemporaryOrder(array $tmpOrder)    &#123;        $key = Uuid::uuid4()-&gt;toString();        Cache::set($key, $tmpOrder, 3);        return $key;    &#125;&#125;```目前的目录解构app/Http/Services/```php├── PayOrder│   ├── PayOrderContext.php│   ├── PayOrderStrategy.php│   └── Strategy│       └── VipStrategy.php└── PayOrderService.php```</code></pre><h3 id="开通-vip-接口"><a href="#开通-vip-接口" class="headerlink" title="开通 vip 接口"></a>开通 vip 接口</h3><p>所有接口的数据都是通过 <code>laravel</code> <a href="https://learnku.com/docs/laravel/5.6/validation/1372#106bee">表单请求验证</a></p><p>路由：routes/web.php</p><pre><code class="php">Route::get(&#39;/buy/vip&#39;, &quot;PayController@vip&quot;)-&gt;name(&#39;vip&#39;);</code></pre><p>app/Http/Controllers/PayController.php</p><pre><code class="php">public function vip(Request $request)&#123;    $strategy = new VipStrategy();    $tmpOrderKey = (new PayOrderContext($strategy))-&gt;createOrder($request);    return $this-&gt;data([&#39;key&#39; =&gt; $tmpOrderKey]);&#125;</code></pre><pre><code class="curl">curl http://127.0.0.1:8000/buy/vip?code=vip1 | json&#123;  &quot;status&quot;: 200,  &quot;message&quot;: &quot;ok&quot;,  &quot;data&quot;: &#123;    &quot;key&quot;: &quot;35349845-0e76-4973-b240-67e7b3cdda42&quot;  &#125;&#125;</code></pre><p>临时订单已生成，现在需要需要开发手机扫码后的预览接口</p><h3 id="预览订单"><a href="#预览订单" class="headerlink" title="预览订单"></a>预览订单</h3><p>正常来说预览订单是每个支付都需要有的功能，所以增加一个抽象方法</p><ol><li><p>在 <code>app/Http/Services/PayOrder/PayOrderStrategy.php</code> 新增一个 <code>preview</code> 的抽象方法</p><pre><code class="php">abstract class PayOrderStrategy&#123;    // 创建临时订单    abstract function createTemporaryOrder(Request $request);       // 预览订单    protected function preview(array $tmpOrder)    &#123;        throw new UnsupportedOperationException(&quot;不支持的方法&quot;);    &#125;&#125;</code></pre><p> 你可能会好奇，这里预览订单为什么要抛出一个异常呢？因为有些第三方支付没有手机支付，只能 pc 端跳转，所以就不会涉及预览这一说</p><p> 如果定义成 <code>abstract</code> 下面继承的方法有必须实现，这个非必须的就直接定义成 <code>protected</code> 并抛出一个异常，开发的时候如果错误的调用了这个方法就会知道，当前支付方式不支持订单的预览</p></li><li><p>开通 vip 策略实现 <code>preview</code> 方法，参数是临时订单的信息</p><p> app/Http/Services/PayOrder/Strategy/VipStrategy.php</p><pre><code class="php">function createTemporaryOrder(Request $request)&#123; /*...*/ &#125;function preview(array $tmpOrder)&#123;    $preview = [        &#39;title&#39;   =&gt; &#39;开通会员&#39;,        &#39;price&#39;   =&gt; $tmpOrder[&#39;price&#39;],        &#39;vip_day&#39; =&gt; $tmpOrder[&#39;vip_day&#39;]    ];    return $preview;&#125;</code></pre></li><li><p>预览订单接口</p><p> 这个接口返回一个页面，手机扫码收可以预览并且有下单按钮</p><p> 路由：routes/web.php</p><pre><code class="php">Route::get(&#39;/buy/preview&#39;, &quot;PayController@preview&quot;)-&gt;name(&#39;preview&#39;);</code></pre><p> app/Http/Controllers/PayController.php</p><pre><code class="php">// 预览订单接口public function preview(Request $request)&#123;    // 请求下单接口后返回的临时订单 key    $tmpOrderKey = $request-&gt;get(&#39;key&#39;);    // 获取临时订单    $tmpOrder = app(PayOrderService::class)-&gt;getTemporaryOrder($tmpOrderKey);    if (!$tmpOrder) &#123;        throw new TemporaryOrderException(&quot;订单已过期&quot;);    &#125;    $strategy = new VipStrategy();    $preview = (new PayOrderContext($strategy))-&gt;preview($tmpOrder);    return view(&#39;preview&#39;, $preview);&#125;</code></pre></li><li><p>生成二维码</p><p> 前端请求 vip 接口之后使用返回的临时订单 key，作为 <code>query</code> 参数请求 <code>预览订单</code> 接口</p><p> <code>http://127.0.0.1:8000/buy/preview?key=35349845-0e76-4973-b240-67e7b3cdda42</code></p>  <div center="left">      <img src="https://cdn.jsdelivr.net/gh/zxr615/md-images/images/2020image-20210317142724525.png" alt="下单二维码" style="zoom:30%" />      <img src="https://cdn.jsdelivr.net/gh/zxr615/md-images/images/2020image-20210317142241260.png" alt="手机确认支付页面" style="zoom:30%;" />  </div></li><li><p>接下来就是立即支付了，但立即支付前还有个问题，预览订单接口的 <code>策略选择</code> 似乎有点问题，这里设计的预览订单接口不论是 <code>开通 vip</code> 还是 <code>充值</code> 都是请求这个接口，所以这里还需要判断一下购买的类型来调用不同的策略。</p><p> 这里用临时订单中的 <code>type</code> 来判断支付的类型，根据 <code>type</code> 来选择策略</p><pre><code class="php">public function preview(Request $request)&#123;    ...    $strategy = new \stdClass();    switch ($tmpOrder[&#39;type&#39;]) &#123;        case PayOrderService::TYPE_VIP:            $strategy = new VipStrategy();            break;        case PayOrderService::TYPE_RECHARGE:            // $strategy = new RechargeStrategy();            break;        // ...    &#125;    $preview  = (new PayOrderContext($strategy))-&gt;preview($tmpOrder);    return view(&#39;preview&#39;, $preview);&#125;</code></pre><p> 好嘛~，问题又来了，这 <code>switch</code> 看着有点不爽，再把它独立出来吧，加一个获取策略的 <code>简单工厂</code> ，接下来优化这段选择策略的代码</p><p> 创建 <code>PreviewFactory</code> 简单工厂</p><p> <code>touch app/Http/Services/PayOrder/PreviewFactory.php</code></p><pre><code class="php">namespace App\Http\Services\PayOrder;use App\Exceptions\BusinessException;use App\Exceptions\TemporaryOrderException;use App\Http\Services\PayOrder\Strategy\VipStrategy;use App\Http\Services\PayOrderService;class PreviewFactory&#123;    public static function strategy(string $key)    &#123;        // 获取临时订单        $tmpOrder = app(PayOrderService::class)-&gt;getTemporaryOrder($key);        if (!$tmpOrder) &#123;            throw new TemporaryOrderException(&quot;订单已过期&quot;);        &#125;        $strategy = new \stdClass();        switch ($tmpOrder[&#39;type&#39;]) &#123;            case PayOrderService::TYPE_VIP:                $strategy = new VipStrategy();                break;            case PayOrderService::TYPE_RECHARGE:                // return new Recharge();                break;            // ...            default:                throw new BusinessException(&#39;订单类型错误.&#39;);        &#125;        return $strategy;    &#125;&#125;</code></pre><p> 现在再来看看 <code>preview</code> 接口</p><pre><code class="php">public function preview(Request $request)&#123;    $tmpOrderKey = $request-&gt;get(&#39;key&#39;);    try &#123;        $preview = PreviewFactory::strategy($tmpOrderKey)-&gt;preview($tmpOrderKey);    &#125; catch (TemporaryOrderException $e) &#123;        return $this-&gt;fail($e-&gt;getMessage());    &#125;    return view(&#39;preview&#39;, $preview);&#125;</code></pre></li></ol><h3 id="发起支付"><a href="#发起支付" class="headerlink" title="发起支付"></a>发起支付</h3><p>和创建订单策略同样，我们也创建一个支付策略</p><p>/Users/tuju/Project/pay/app/Http/Services/Payment</p><pre><code class="tree">Payment├── PaymentContext.php├── PaymentFactory.php├── PaymentStrategy.php└── Strategy    ├── AlipayStrategy.php    ├── UnionStrategy.php    └── WechatStrategy.php</code></pre><p>定义支付接口</p><p>PaymentStrategy.php</p><pre><code>interface PaymentStrategy&#123;    public function pay(array $order);&#125;</code></pre><p>上下文联系</p><p> PaymentContext.php</p><pre><code class="php">class PaymentContext&#123;    private $strategy;    public function __construct(PaymentStrategy $paymentStrategy)    &#123;        return $this-&gt;strategy = $paymentStrategy;    &#125;    public function pay(array $order)    &#123;        return $this-&gt;strategy-&gt;pay($order);    &#125;&#125;</code></pre><p>获取支付策略的工厂</p><p>PaymentFactory.php</p><pre><code>class PaymentFactory&#123;    public static function strategy(string $payType)    &#123;        switch ($payType) &#123;            case &#39;wechat&#39;:                $strategy = new WechatStrategy();                break;            case &#39;alipay&#39;:                $strategy = new AlipayStrategy();                break;            case &#39;union&#39;:                $strategy = new UnionStrategy();                break;            // case...            default:                throw new BusinessException(&quot;支付方式不存在&quot;);        &#125;        return $strategy;    &#125;&#125;</code></pre><p>支付宝策略<br>Strategy/AlipayStrategy.php</p><pre><code class="php">class AlipayStrategy implements PaymentStrategy&#123;    public function pay(array $order)    &#123;        /**         * 向支付宝请求         * @see 支付宝官方sdk https://github.com/alipay/alipay-easysdk/tree/master/php         * @see 第三方sdk https://github.com/lokielse/omnipay-alipay         */        return &quot;https://www.alipay.com/&quot;;    &#125;&#125;</code></pre><p>微信策略</p><p>Strategy/WechatStrategy.php</p><pre><code class="php">class WechatStrategy implements PaymentStrategy&#123;    public function pay(array $order)    &#123;        /**         *         * @see 微信官方 https://github.com/wechatpay-apiv3/wechatpay-guzzle-middleware         * @see 官方文档 https://pay.weixin.qq.com/wiki/doc/apiv3/open/pay/chapter2_6_2.shtml         * @see 第三方sdk https://github.com/lokielse/omnipay-wechatpay         */        return &quot;https://pay.weixin.qq.com/&quot;;    &#125;&#125;</code></pre><p>确认支付接口</p><p>app/Http/Controllers/PayController.php</p><pre><code class="php">public function pay(Request $request)&#123;    $tmpOrderKey = $request-&gt;get(&#39;key&#39;);    // pay_type=wechat|alipay|union    $payType = $request-&gt;get(&#39;pay_type&#39;);    // 处理订单数据、创建订单    $tmpOrder = app(PayOrderService::class)-&gt;getTemporaryOrder($tmpOrderKey);    $order = [ /** ... */];    $created = app(PayOrderService::class)-&gt;createOrder($order);    if (!$created) &#123;        return $this-&gt;fail(&quot;支付失败, 请重新生成订单.&quot;);    &#125;    // 发起支付    try &#123;          // 前面我们定义了一个支付策略工厂模式，帮助我们实例化策略，所以这里传入我们的支付方式        // 工厂就会帮我们对应支付策略返回回来，然后我们再统一调用 pay() 这个方法        $strategy = PaymentFactory::strategy($payType);        // 一般第三方会返回一个支付跳转链接，点击确认支付的时候用户是已经在手机页面了        // 所以直接跳转链接就可以拉起对应的支付了。        $url = (new PaymentContext($strategy))-&gt;pay($created);    &#125; catch (BusinessException $e) &#123;        $this-&gt;fail($e-&gt;getMessage());    &#125;    // 跳转    return redirect($url);&#125;</code></pre><p>最后列出下最终策略模块的树状图</p><pre><code>├── PayOrder 支付相关策略集合│   ├── PayOrderContext.php│   ├── PayOrderStrategy.php│   ├── PreviewFactory.php│   └── Strategy 具体策略，如果要充值，则新建一个充值策略即可，新增的方式也不会影响到开通会员的相关功能│       └── VipStrategy.php├── PayOrderService.php 一些公用方法└── Payment 支付相关策略集合    ├── PaymentContext.php    ├── PaymentFactory.php    ├── PaymentStrategy.php    └── Strategy 具体策略，可以增加各种第三方支付        ├── AlipayStrategy.php        ├── UnionStrategy.php        └── WechatStrategy.php</code></pre><h2 id="解决的问题"><a href="#解决的问题" class="headerlink" title="解决的问题"></a>解决的问题</h2><ul><li><p>将接口细分，不再是所有订单都进入同一个方法，解决了参数混乱问题</p></li><li><p>把大杂烩 <code>$data</code> 中的数据全部切分到每个不同的策略中去，而不是在方法中使用大量的 <code>if</code> 和 <code>switch</code> 来处理，再增加类型时只需要关注新增的策略即可</p></li><li><p>把接口数据用 <code>laravel</code> <a href="https://learnku.com/docs/laravel/5.6/validation/1372#106bee">表单请求验证</a> 来判断，而不是在 <code>controller</code> 和  <code>service</code> 层用 <code>if</code> 来判断</p></li><li><p>把 <code>\Exception</code> 代码全部替换成相应的业务异常</p></li></ul><h2 id="不足之处"><a href="#不足之处" class="headerlink" title="不足之处"></a>不足之处</h2><ul><li>创建订单中的零时订单存入到 redis 后再获取，还是不能明确知道数组里具体存入了什么数据，在 GO 中在序列化 <code>json</code> 时需要一个 <code>struct</code> 来支持，明确表名 json 中有什么字段，这样开发的时候会减少很多查看代码的时间，我认为可以新建一个 class 来模拟 GO 中的 <code>struct</code> 来明确 json 里面有什么数据。</li></ul>]]></content>
      
      
      <categories>
          
          <category> PHP </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Laravel </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
